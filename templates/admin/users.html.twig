{% extends 'base.html.twig' %}

{% block title %}Lista użytkowników{% endblock %}

{% block body %}

    <h3>{% trans %}Użytkownicy{% endtrans %}</h3>

    <div class="table-responsive">
        <table id="usersTable" class="table table-striped">
            <thead>
            <tr>
                <th>{% trans %}Login{% endtrans %}</th>
                <th>{% trans %}Imię i nazwisko{% endtrans %}</th>
                <th>{% trans %}Email{% endtrans %}</th>
                <th>{% trans %}Data rejestracji{% endtrans %}</th>
                <th>{% trans %}Ostatnie logowanie{% endtrans %}</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for user in pagination %}
                <tr>
                    <td>{{ user.username }}</td>
                    <td>{{ user.name~' '~user.surname}}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.createdAt|date('d/m/Y H:i') }}</td>
                    <td>{{ user.lastVisit|date('d/m/Y H:i') }}</td>

                    <td>
                        <div class="btn-group btn-group-sm">
                            <a href="{{ path('app_admin_user_show',{id: user.id}) }}" class="btn btn-sm btn-outline-primary mb-2">{% trans %}Zobacz{% endtrans %}</a>
                            {#<a href="{{ path('app_admin_user_edit',{id:user.id}) }}" class="btn btn-sm btn-outline-secondary mb-2">{% trans %}Edytuj{% endtrans %}</a>#}
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}
