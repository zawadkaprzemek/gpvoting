<header class="bg-dark">
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="collapse navbar-collapse flex-grow-1 text-right" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto flex-nowrap">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {% if app.user %}
                            {% trans %}logged.as{% endtrans %}: {{ app.user.userIdentifier }}
                            {% else %}
                                {% trans %}guest{% endtrans %}
                            {% endif %}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            {% if app.user %}
                                {% if is_granted("ROLE_ADMIN") %}
                                    <a class="dropdown-item" href="{{ path('app_admin_create_account') }}">{% trans %}admin.create_user{% endtrans %}</a>
                                    <a class="dropdown-item" href="{{ path('admin_users_list') }}">{% trans %}admin.users_list{% endtrans %}</a>
                                    <a class="dropdown-item" href="{{ path('admin_packs_list') }}">{% trans %}admin.packs_list{% endtrans %}</a>
                                {% endif %}
                                <a class="dropdown-item" href="{{ path('app_profile')}}">{% trans %}profile.my{% endtrans %}</a>
                                <a class="dropdown-item" href="{{ path('app_profile_edit')}}">{% trans %}profile.edit{% endtrans %}</a>
                                <a class="dropdown-item" href="{{ path('app_profile_change_password')}}">{% trans %}profile.change_password{% endtrans %}</a>
                                <a class="dropdown-item" href="#">{% trans %}profile.settings{% endtrans %}</a>
                                <a class="dropdown-item" href="{{path('app_logout')}}">{% trans %}logout{% endtrans %}</a>
                            {% else %}
                                <a class="dropdown-item" href="{{ path('app_login')}}">{% trans %}login{% endtrans %}</a>
                                <a class="dropdown-item" href="{{path('app_register')}}">{% trans %}registration{% endtrans %}</a>
                            {% endif %}

                        </div>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
</header>
<header class="bg-light">
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-light">
            <a class="navbar-brand" href="{{path('app_event_list')}}"><img src="{{ asset('assets/images/gpvo-logo.png') }}" alt="GP Voting Online" style="max-width: 220px;" /></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        {% if is_granted("ROLE_USER") %}
            <div class="collapse navbar-collapse flex-grow-1 text-right" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto flex-nowrap text-uppercase font-weight-bold">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_instruction')}}">{% trans %}instruction{% endtrans %}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_event_list')}}">{% trans %}preview{% endtrans %}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_manage')}}">{%trans%}manage.text{% endtrans %}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_pollings')}}">{% trans %}voting{% endtrans %}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">{% trans %}analysis.text{% endtrans %}</a>
                    </li>
                </ul>{#
                <form class="form-inline my-2 my-lg-0">
                    <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                </form>#}
            </div>
            {% endif %}
        </nav>
    </div>
</header>